info:
  name: SignUp
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:5000/api/auth/signup
  body:
    type: json
    data: |-
      {
        "first_name": "ML_test1",
        "last_name": "Tan",
        "email": "test1@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
