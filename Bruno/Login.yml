info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:5000/api/auth/login
  body:
    type: json
    data: |-
      {
        "username": "tml@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
